# Copy this file to .env (or set these in the repo-root .env) and fill in the values.
# Both BIFROST_URL and WORKSHOP_VK are also set in the repo-root .env.example;
# whichever Nuxt sees first wins.

BIFROST_URL=http://localhost:8080
WORKSHOP_VK=
