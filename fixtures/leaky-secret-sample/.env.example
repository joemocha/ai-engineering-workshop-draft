# Workshop fixture .env.example — illustrates what a leaky env might look like.
# Real values are NOT used; the keys live in source files instead (which is exactly
# the anti-pattern this fixture is designed to test against).

OPENAI_API_KEY=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
